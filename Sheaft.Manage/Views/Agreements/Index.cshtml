@model IEnumerable<AgreementViewModel>
@{
    ViewData["Title"] = "Agreements";
    int take = ViewBag.Take;
    int currentPage = ViewBag.Page;
}

<div class="container">
    <h1 class="display-4">Agreements</h1>
    <br />

    @if (ViewBag.Removed != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <span>Successfully removed agreement @ViewBag.Removed</span>

            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <br />
    }

    @if (ViewBag.Edited != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <span>Successfully updated agreement @ViewBag.Edited.Name</span>
            <a asp-controller="Agreements" asp-action="Edit" asp-route-id="@ViewBag.Edited.Id">View details</a>

            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <br />
    }

    <table class="table table-hover">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Producer</th>
                <th scope="col">Store</th>
                <th scope="col">Status</th>
                <th scope="col">CreatedOn</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var entity in Model)
            {
                <tr>
                    <th scope="row">@entity.Id</th>
                    <td>@entity.Delivery.Producer.Name</td>
                    <td>@entity.Store.Name</td>
                    <td>@entity.Status</td>
                    <td>@entity.CreatedOn</td>
                    <td>
                        <a asp-controller="Agreements" asp-action="Edit" asp-route-id="@entity.Id" class="btn btn-info">
                            <i class="fa fa-pen"></i>
                        </a>
                        @if (User.IsInRole("ADMIN"))
                        {
                            @using (Html.BeginForm("Delete", "Agreements", new { id = entity.Id }, FormMethod.Post, true, new { @class = "form d-inline" }))
                            {
                                <button type="submit" class="btn btn-danger">
                                    <i class="fa fa-trash"></i>
                                </button>
                            }
                        }
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr class="text-center">
                <td colspan="4">
                    @if (currentPage >= 1)
                    {
                        <a asp-controller="Agreements" asp-action="Index" asp-route-take="@take" asp-route-page="@(currentPage-1)" class="btn btn-info">
                            <i class="fa fa-caret-left"></i>
                        </a>
                    }
                    @if (take == Model.Count())
                    {
                        <a asp-controller="Agreements" asp-action="Index" asp-route-take="@take" asp-route-page="@(currentPage+1)" class="btn btn-info">
                            <i class="fa fa-caret-right"></i>
                        </a>
                    }
                </td>
            </tr>
        </tfoot>
    </table>
</div>
