export enum DayOfWeek {
  Sunday,
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
}

export enum AgreementStatus {
  Pending,
  Active,
  Refused,
  Cancelled
}
export enum AgreementOwner {
  Supplier,
  Customer
}
